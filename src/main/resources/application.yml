spring:
  datasource:
    url: jdbc:h2:tcp://localhost/C:\Users\he2joo\Desktop\Spring\jpashop\jpashop;
    username: sa
    password:
    driver-class-name: org.h2.Driver

  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
#        show_sql: true
        format_sql: true #show_sql은 한줄로 보여준다면 format_sql은 예쁘게 보여줌

logging:
  level:
    org.hibernate.SQL: debug #쿼리 보기
#    org.hibernate.type: trace # 쿼리 파라미터 보기
